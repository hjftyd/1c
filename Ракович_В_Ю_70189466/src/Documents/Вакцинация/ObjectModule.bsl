Процедура ОбработкаПроведения(Отказ, РежимПроведения)
    
    // Регистр сведений "КартаПрививок"
    Если Статус = Перечисления.СтатусыВакцинации.Выполнена Тогда
        Запись = РегистрыСведений.КартаПрививок.СоздатьЗапись();
        Запись.Студент = Студент;
        Запись.Прививка = Прививка;
        Запись.ДатаВакцинации = Дата;
        Запись.СерияВакцины = СерияВакцины;
        Запись.Реакция = Реакция;
        Запись.Записать();
    КонецЕсли;
    
КонецПроцедуры 