// Проверка значения на соответствие норме
Функция ПроверитьЗначение(Значение, Возраст = Неопределено) Экспорт
    
    Если Значение < КритическийМин Тогда
        Возврат "Критически низкое";
    ИначеЕсли Значение < НормаМин Тогда
        Возврат "Ниже нормы";
    ИначеЕсли Значение > НормаМакс И Значение <= КритическийМакс Тогда
        Возврат "Выше нормы";
    ИначеЕсли Значение > КритическийМакс Тогда
        Возврат "Критически высокое";
    Иначе
        Возврат "В норме";
    КонецЕсли;
    
КонецФункции

// Расчет отклонения от нормы в процентах
Функция РассчитатьОтклонение(Значение) Экспорт
    
    Если Значение < НормаМин Тогда
        Возврат (Значение - НормаМин) / НормаМин * 100;
    ИначеЕсли Значение > НормаМакс Тогда
        Возврат (Значение - НормаМакс) / НормаМакс * 100;
    Иначе
        Возврат 0;
    КонецЕсли;
    
КонецФункции