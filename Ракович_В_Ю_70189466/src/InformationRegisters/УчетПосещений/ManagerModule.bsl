// Получить посещения студента за период
Функция ПолучитьПосещенияСтудента(Студент, ДатаНачала, ДатаОкончания) Экспорт
    
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ
    |    УчетПосещений.ДатаПосещения,
    |    УчетПосещений.ВремяПрихода,
    |    УчетПосещений.ВремяУхода,
    |    УчетПосещений.Причина,
    |    УчетПосещений.Температура,
    |    УчетПосещений.МедРаботник
    |ИЗ
    |    РегистрСведений.УчетПосещений КАК УчетПосещений
    |ГДЕ
    |    УчетПосещений.Студент = &Студент
    |    И УчетПосещений.ДатаПосещения МЕЖДУ &ДатаНачала И &ДатаОкончания
    |УПОРЯДОЧИТЬ ПО
    |    УчетПосещений.ДатаПосещения УБЫВ,
    |    УчетПосещений.ВремяПрихода УБЫВ";
    
    Запрос.УстановитьПараметр("Студент", Студент);
    Запрос.УстановитьПараметр("ДатаНачала", ДатаНачала);
    Запрос.УстановитьПараметр("ДатаОкончания", ДатаОкончания);
    
    Возврат Запрос.Выполнить().Выгрузить();
    
КонецФункции

// Расчет времени приема
Функция РассчитатьВремяПриема(ВремяНачала, ВремяОкончания) Экспорт
    
    Если ВремяНачала = Неопределено Или ВремяОкончания = Неопределено Тогда
        Возврат 0;
    КонецЕсли;
    
    Разница = ВремяОкончания - ВремяНачала;
    Возврат Цел(Разница * 24 * 60); // в минутах
    
КонецФункции
